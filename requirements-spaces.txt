# Requirements for Hugging Face Spaces deployment.
# Uses docling for PDF parsing but with a lightweight pipeline (no OCR, no
# TableFormer ML model) — IDX annual reports are digital PDFs so text is
# embedded; the fast path is sufficient and avoids downloading 1-2 GB models.
# Dense retrieval (sentence-transformers, chromadb) is excluded; DocumentStore
# falls back gracefully to BM25-only retrieval.

# --- PDF parsing ---
docling>=2.0.0

# --- Agent orchestration ---
langgraph>=0.2.50
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# --- LLM providers ---
langchain-groq>=0.2.0
langchain-google-genai>=2.0.0
langchain-openai>=0.2.0

# --- Financial data ---
yfinance>=0.2.50
pandas>=2.0.0
numpy>=1.26.0

# --- Retrieval (sparse only — no embedding model) ---
rank-bm25>=0.2.2

# --- News ---
ddgs>=6.0.0                      # free fallback
tavily-python>=0.5.0            # reliable when TAVILY_API_KEY secret is set

# --- Output validation ---
pydantic>=2.0.0

# --- UI ---
gradio>=5.0.0

# --- Utils ---
python-dotenv>=1.0.0
